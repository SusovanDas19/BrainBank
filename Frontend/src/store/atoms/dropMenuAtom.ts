import { atom } from "recoil";

export const dropMenuAtom = atom({
    key: "dropMenuAtom",
    default: false
})

export const swtichMenuAtom = atom({
    key: "swtichMenuAtom",
    default: false
})